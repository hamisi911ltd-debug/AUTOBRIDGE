// Re-exported from lib/brand.ts, the single source of truth for every
// branding constant (Ferbil vs. the blank white-label template build) -
// kept as its own module so every existing `from "@/lib/company"` import
// stays valid.
export { COMPANY, BANK_DETAILS } from "@/lib/brand";
